# A 股分析与预测算法审查及改进记录

日期：2026-09-12。范围：个股评分、成本分布、数据口径、交易评估、冰点概率及关联展示。

## 结论

当前系统主要是技术规则评分、历史统计与研究看板，而不是已经证明具有稳定超额收益的预测系统。此次优先修正会改变统计结论的实现问题，再引入可被检验的简单概率模型；没有用增加指标或神经网络复杂度代替数据纪律。

本次代码通过离线正确性验证，**没有完成新版真实行情样本外业绩检验**。不应把下列修正解释为胜率提高、收益提升或可以直接实盘。未启动／重启服务，未联网获取行情，未覆盖已有行情缓存、旧评估报告或校准文件，未提交 Git。

## 一、确认的问题与处理

| 问题 | 实际影响 | 此次处理 |
| --- | --- | --- |
| position 维度关闭后有效权重未归一化 | 中性 50 分被压到约 35–40，信号阈值的意义改变 | 禁用维度置零后归一化，返回实际权重；保留阈值但标明未经重新验证 |
| 启发式分数称为上涨概率 | 用户可能把 60 分误解成 60% 胜率 | 新增 bullish_score/bearish_score，标记 uncalibrated；界面改为“分”，旧 probability 字段只供兼容 |
| 当前流通股本用于全部历史换手 | 后发生的股本变化影响历史筹码，截断 OHLC 无法消除上游时点污染 | 明确换手来源；仅可信历史换手可用于衰减，其余退回历史成交量分布 |
| 零成交／零换手仍迁移筹码 | 停牌或真实零换手日制造新筹码 | 零成交不衰减、不注入，真实零／微小换手不强制最低 0.5% |
| 前复权与不复权静默回退 | 除权跳变进入指标，日周可采用不同尺度 | 保留回退但标记来源和复权；严格评估排除 raw/unknown；在线日周不一致时禁用周线混合评分 |
| 入场当天允许止损 | 违反普通 A 股 T+1 | 入场日不可卖，触发后保留待卖，后续可成交日执行 |
| 跌停未成交及期末强制平仓 | 制造实际上无法取得的退出价格 | 封板延期，待卖单持续有效，数据耗尽标为截尾；固定期收益明确为 MTM 而非可执行收益 |
| 原 PAVA 对逐点重复合并权重 | 不是正确的加权保序最小二乘解 | 重写为整块合并并用穷举最优解检验；不再默认使用单调假设作为线上概率约束 |
| 空分箱被补成零命中率参与拟合 | 无数据也输出貌似精确的概率 | 小于 30 样本的分箱不输出概率，空分箱也无区间 |
| 融资缺失可变成“零冰度”或无限沿用旧值 | 推理特征与训练条件不一致 | 滚动百分位排除 NaN 分母、当前缺失保留 NaN；缺关键特征拒绝套用校准表 |
| 训练 800 行／推理 300 行 | ret60 的 250 日百分位需要至少约 310 行，最新分数可能不一致 | 推理使用与校准一致的 800 行请求 |
| MA200 在 dropna 后才计算 | 额外丢失历史，早期体制被错误归入空头 | 在原始完整序列先算均线，未知体制不归类 |
| 未到期标签被转为 0 | 若以后复用标签可能把未知当作未反弹 | 未来 10 根未知标签保持 NaN |
| 日线盘中值混入收盘训练定义 | 量能等特征分布不匹配 | 上海时间 15:10 前剔除当日未完成日线 |
| 旧磁盘模型和旧成功概率反复复用 | 修算法后可能继续显示旧模型；刷新失败留下旧概率 | 校准版本／指数／文件 TTL 检查，校准日期与行情日期核对；预测过期同步重算，失败清除旧成功缓存 |

### 旧报告透露了什么

`backend/eval_reports/latest.md` 生成于 2026-09-03，使用 39 只股票和 5575 个可成交评估点。旧模型综合评分逐日 IC 均值约 0.0181、报告 t 值约 0.85；所谓“31.1% 概率”分组实际命中 51.9%，“61.6% 概率”分组实际命中 50.6%。这足以说明旧分数不能直接解释为概率，但不是新版模型的表现。

这些旧统计还受到样本相关、选股池及执行近似限制，不能把报告中的普通 t 值／Wilson 区间当作严格独立样本证据。已保留旧文件以免伪造重新评估结果；旧文档描述与本记录冲突时，以代码和本记录为准。

## 二、已实现的新冰点概率模型

实现：`backend/probability_calibration.py`；集成：`backend/ice_engine.py`。

### 1. 固定事件与模型

- 事件：信号日 T 收盘到 T+10 收盘，指数涨幅 ≥2.5%。
- 这是指数价格事件，不是个股胜率，也不是 ETF 扣成本后的收益事件。
- 另保留 T+1 开盘到 T+10 收盘的**毛收益**历史描述，未计手续费、滑点和跟踪误差；指数本身不可直接交易。
- 六项冰点分特征及现有权重仍为规则设定，没有声称这些权重最优。
- 固定五箱：[0,20)、[20,40)、[40,60)、[60,80)、[80,100]。

用每箱历史命中数 k、样本数 n、训练集平滑基率 p0 估计：

```text
p0 = (全训练命中数 + 1) / (全训练样本数 + 2)
p_bin = (k + 20 × p0) / (n + 20)
```

这是经验贝叶斯风格的向基率收缩估计。20 是预先固定的收缩强度，不是本次搜索得到的最优值；少样本估计向基率靠拢。线上 n < 30 不输出概率。不强制“越冰反弹概率越大”，因为单调关系本身需要证据。

### 2. 按时间推进的样本外诊断

- 使用原始交易日位置，而不是删掉 NaN 后的压缩行号。
- 对测试信号位置 t，训练样本必须满足 `signal_position + 10 < t`，即标签已在此前完全到期。
- 至少 120 个完整训练样本后开始预测；训练集逐步扩大。
- 测试信号间隔至少 11 个交易日，避免 10 日事件窗口重叠；训练标签仍可重叠，不宣称全部样本独立。
- 输出 Brier、对照基率 Brier、Brier skill、log loss、对照基率 log loss，并保留预测时点及训练标签截止位置，便于复核。
- `insufficient_oos`：少于 60 个样本外测试点。
- `no_oos_edge`：达到测试点门槛但 Brier 未优于同期历史基率。
- `positive_oos_skill`：本批测试 Brier 优于基率，不表示统计显著、稳定收益或可直接交易。

使用 800 行历史时，考虑指标预热及标签清洗，通常不足 60 个不重叠样本外点；面板出现“样本外验证不足”是预期且诚实的结果，而不是错误。更长历史应由可追溯数据存储补齐，不能只把请求 count 改大就假定供应商提供了足够记录。

### 3. 区间与展示的边界

原有 `n/10` Wilson 区间保留为**原始分箱命中率的启发式参考**，不是收缩模型后验区间，也没有严格“去重叠保守覆盖率”保证，点估计甚至可能因收缩落在原始分箱区间外。前后端均明确披露这一点。

接口新增 `model_version`、`asof_date`、`probability_status`、`validation`、`ci_method`。少样本或缺失特征时不伪造概率。旧版本校准表会在下一次应用调用时失效并尝试重新生成；此次没有提前写入真实行情校准产物。

## 三、个股与执行约束说明

### 个股评分

新增明确的多空评分字段；`bullish_probability`／`bearish_probability` 暂保留旧数值类型及截断方式以降低 API 破坏，但它们不是概率，外部调用方应迁移到 score。多空评分也不是两个已经定义互补事件的概率。

权重修复和筹码降级会改变评分分布、支撑压力位与信号数量。原 70/75 等阈值未重调，不应直接据此扩大仓位。在线“相似形态”模块仍为信号日收盘入场的样本内路径描述，不是独立样本外验证或完整可交易回测。

### 日线退出近似

`path_exit` 实现 T+1、跳空、待卖及一字跌停延期；有 high 时识别 OHLC 一字跌停，无 high 时采用明确的保守启发式。买入日触及止损的委托在下一可卖日仍有效，即使次日反弹也按开盘退出。

日线不能确认涨跌停排队、盘中先后顺序或实际开盘成交量。ST／历史涨跌停制度、上市初期、停牌、退市、税费变化与价格最小档位仍未完整建模。延期退出会超过名义持有期；截尾样本排除虽避免伪造收益，仍可能造成选择偏差，不能当作组合收益。

固定期 `ret_h` 是计划时点的盯市收益（MTM），不是强行保证当天能卖出的成交收益。与延期止损路径的差值不能直接解释为同期限策略增益。

## 四、更好的研究模型建议

### 优先级 1：先把研究样本变得可信

建议先建设有版本的点时间数据集：原始价格、复权因子及公司行动、历史流通股本、证券状态和上市／退市／ST 历史、交易日历、停复牌、可交易股票池、融资实际披露时间。分别记录 observation_date 与 available_at，保存抓取时间、供应商和版本。

当前 qfq 标记只防止静默混用，不等于已经解决未来复权因子、分红总收益或历史股本时点问题。当前融资的滞后一日假设仍需要对照供应商真实披露时间。

### 优先级 2：横截面排名，先于复杂方向预测

对个股，更建议先预测未来 5/10/20 日**相对行业或市场的超额收益排序**，而不是精确价格：

1. 正则化线性／逻辑回归作可解释基线，输入趋势、反转、波动、量价、流动性等少量稳定特征。
2. 数据足够后，用 LightGBM 的回归或排序模型作为挑战者；与线性基线做同样的时间隔离比较。
3. 按交易日期切分，所有股票共用同一时间边界，不做随机行拆分；训练边界清除未到期标签，调参使用嵌套滚动验证。
4. 若需要“概率”，应先固定事件（例如扣成本后超额收益 >0），再在独立时序校准段做 sigmoid／isotonic 校准，最后在未参与选择的测试段检验。

本次没有直接接入 LightGBM、LSTM 或 Transformer：当前数据长度、覆盖范围与执行偏差不足以支持更复杂模型的收益结论，盲目加入只会扩大过拟合空间。已实现的收缩基线及时间隔离工具是后续挑战者的比较标准。

### 优先级 3：以组合和样本外证据决定是否上线

- 概率：Brier skill、log loss、可靠性图、相对当时可知基率的提升。
- 排名：逐日 Rank-IC、行业中性后的 IC、分位组合收益、换手与交易容量。
- 交易：扣真实成本收益、回撤、资金占用、无法成交比例、延期退出和截尾数量。
- 不确定性：按日期聚合的 block bootstrap／HAC；不是把同日多股票与重叠收益当作独立样本。
- 模型选择：保留最终时间段只做一次确认检验；若频繁试模型，要处理多重比较及选择偏差。
- 在数据或有效样本不足时允许“不输出”，而不是每次都给出确定买卖结论。

## 五、验证记录与复现

在 Windows、Python 3.12.8 的当前工作区运行：

```bash
python -B -m unittest discover -s backend -p 'test_*regressions.py'
node --check frontend/app.js
node frontend/test_display_regressions.cjs
git diff --check
```

结果：53 项 Python 离线回归测试全部通过；JavaScript 语法检查和 Node VM 展示契约测试通过；差异空白检查通过（Git 仅提示部分文件 LF/CRLF 转换）。测试中出现的腾讯取数失败日志是模拟回退场景，不是实际联网失败。

测试覆盖：整块 PAVA 与穷举最优解、缺失／空分箱、未来标签扰动不影响过去预测、标签到期隔离、测试窗口不重叠、训练推理窗口一致、盘中日线排除、模型版本失效、旧预测失效、T+1／跳空／跌停／延期／截尾、评分与成本分布、数据来源及复权冲突、前端零分和旧字段兼容、缺失概率／请求失败清空等。

未执行：真实行情回测、新版实证校准、组合级回测、浏览器视觉验收、长期服务调度验证。离线测试与假行情结果不能代替上述验证。
