# A股多维技术指标与支撑压力位走势预测系统：算法原理与系统实现架构

> **版本**: v2.4 修正版  
> **更新日期**: 2026-09-02  
> **改进摘要**: v2.3 基础上修复 MRDI 多周期决策链路、大盘 G 分 MACD 重复计分、操作许可分支顺序、聚类 boll_mid 双计；后端新增 `timeframes` / `daily_kline_full` 供 MRDI 与前端多周期矩阵直接使用

---

## 目录
1. [系统总体架构设计](#1-系统总体架构设计)
2. [数据层：免Token多通道聚合与实时行情动态融合引擎](#2-数据层免token多通道聚合与实时行情动态融合引擎)
3. [多维技术指标与特征提取算法体系](#3-多维技术指标与特征提取算法体系)
   - 3.1 [筹码衰减分布模型 (Volume Profile & Chip Distribution)](#31-筹码衰减分布模型-volume-profile--chip-distribution)
   - 3.2 [形态几何与空间分割 (Price Structure & Fibonacci)](#32-形态几何与空间分割-price-structure--fibonacci)
   - 3.3 [动态均线与通道系统 (Dynamic Moving Levels)](#33-动态均线与通道系统-dynamic-moving-levels)
   - 3.4 [量价异动与动量背离 (Momentum & Divergence)](#34-量价异动与动量背离-momentum--divergence)
   - 3.5 [量价特征分析引擎 (Volume-Price Feature Analysis)](#35-量价特征分析引擎-volume-price-feature-analysis) *(v2.0新增)*
4. [多维支撑位/压力位"价格带聚类共振算法"](#4-多维支撑位压力位价格带聚类共振算法)
5. [大概率走势预测与量化交易计划生成引擎](#5-大概率走势预测与量化交易计划生成引擎)
   - 5.1 [周K+日K双周期共振模型](#51-周k日k双周期共振模型)
   - 5.2 [自适应四维特征量化评分机制](#52-自适应四维特征量化评分机制) *(v2.0改进)*
   - 5.3 [严格历史相似形态胜率回溯统计 (Historical Backtest)](#53-严格历史相似形态胜率回溯统计-historical-backtest) *(v2.0改进)*
   - 5.4 [结构化量化交易计划卡 (Trade Plan) 生成](#54-结构化量化交易计划卡-trade-plan-生成) *(v2.0改进)*
6. [全A股盘后批量扫描与选股雷达](#6-全a股盘后批量扫描与选股雷达)
7. [大盘核心指数多周期走势研判与操作许可量化系统](#7-大盘核心指数多周期走势研判与操作许可量化系统)
   - 7.1 [四大周期 (30分/60分/日线/周线) 结构剖析](#71-四大周期-30分60分日线周线-结构剖析)
   - 7.2 [周期价格斜率 (ATR/根) 与方向分 G 评分模型](#72-周期价格斜率-atr根-与方向分-g-评分模型)
   - 7.3 [反弹条件与回调风险量化评分](#73-反弹条件与回调风险量化评分)
   - 7.4 [正式操作许可与量化建议仓位决策矩阵](#74-正式操作许可与量化建议仓位决策矩阵)
   - 7.5 [大盘专属支撑/压力位与防守底线](#75-大盘专属支撑压力位与防守底线)
   - 7.6 [多周期方向摘要 API (`timeframes`) 与 MRDI 数据契约](#76-多周期方向摘要-api-timeframes-与-mrdi-数据契约) *(v2.4新增)*
8. [系统工程实现与可视化看板交付](#8-系统工程实现与可视化看板交付)
   - 8.4 [MRDI 动量共振与多周期决策面板](#84-mrdi-动量共振与多周期决策面板) *(v2.4新增)*
9. [v2.0 改进日志](#9-v20-改进日志)
10. [v2.1 修正日志](#10-v21-修正日志-2026-08-30)
11. [v2.2 修正日志](#11-v22-修正日志-2026-08-31)
12. [v2.3 大盘支撑/压力升级](#12-v23-大盘核心指数支撑压力精确计算与模型升级-2026-08-31)
13. [v2.4 MRDI 多周期闭环修正](#13-v24-mrdi-多周期闭环与算法一致性修正-2026-09-02)

---

## 1. 系统总体架构设计

本系统针对中国 A 股市场（T+1 制度、10%/20% 涨跌停限制、主力资金引导、筹码沉淀等特性），采用 **"多维特征提取 $\to$ 价格带聚类共振 $\to$ 双周期趋势确认 $\to$ 历史回测胜率预测 $\to$ 结构化交易决策"** 的量化流程，并提供 **个股量化看板**、**大盘多周期指数研判** 与 **MRDI 动量共振决策** 三看板协同作业（v2.4）。

```mermaid
flowchart TD
    subgraph Data [1. 数据接入与实时融合]
        D1[腾讯/新浪免Token行情通道] --> D2[实时分时与日周K线]
        D2 --> D3[盘中实时快照动态注入融合 (保证100%当天最新)]
        D3 --> D3b[真实流通股本换手率计算 v2.0]
    end

    subgraph Indicator [2. 多维指标与特征引擎]
        D3b --> I1[筹码衰减分布模型: POC/获利盘/集中度 (涨跌停降权)]
        D3b --> I2[形态空间: 顶底分型/缺口/自适应斐波那契]
        D3b --> I3[动态通道: MA均线组/布林带/ATR]
        D3b --> I4[动量量价: MACD-DIF极值背离/KDJ(趋势过滤)]
        D3b --> I5[量价特征: 放量突破/缩量回踩/量价背离 v2.0]
    end

    subgraph Confluence [3. 支撑/压力带聚类共振]
        I1 & I2 & I3 & I4 & I5 --> C1[容差聚类: 1.5%价格邻域合并]
        C1 --> C2[加权共振评分: 输出S1-S3与R1-R3及星级]
    end

    subgraph MacroIndex [4. 大盘多周期指数研判]
        D3b --> M1[四大周期切片: 30分/60分/日K/周K (完整OHLC)]
        M1 --> M2[周期价格斜率 ATR/根 + 方向分 G + 风险量化]
        M2 --> M3[正式操作许可: 战略方向 + 建议总仓位 + 防守底线]
    end

    subgraph Prediction [5. 个股胜率预测与交易计划]
        C2 --> P1[周K+日K双周期共振]
        P1 --> P2[自适应四维量化评分: 趋势/筹码/动量/空间 (动态权重)]
        P2 --> P3[严格历史回测: 收盘价判断+扣除交易成本 v2.0]
        P3 --> P4[交易计划: ATR动态止损/建仓区间/盈亏比]
    end

    subgraph Interface [6. 三视图可视化看板]
        P4 --> UI1[⚡ 个股量化看板: K线主副图/筹码分布/雷达池]
        M3 --> UI2[🌐 大盘多周期研判: 核心结论/四联卡片/指数K线]
        M3 --> UI3[📊 MRDI 动量共振: RD/CD/G/五级决策矩阵 v2.4]
    end
```

---

## 2. 数据层：免Token多通道聚合与实时行情动态融合引擎

- **实现目标**：开箱即用，无需配置 API Token 或付费账号，毫秒级获取全市场当天最新行情。
- **实时数据融合算法 (Real-time Merge Engine)**：
  - **K线主通道**：采用腾讯前复权K线接口（`web.ifzq.gtimg.cn/appstock/app/fqkline/get`，`qfq` 前复权，成交量单位为**手**），自动处理分红送转除权，杜绝除权缺口被误判为跳空缺口。🆕 **v2.1**：旧版使用新浪不复权接口，除权日价格跳空会污染缺口识别与筹码分布。
  - **K线回退通道**：腾讯接口异常时回退新浪财经K线接口（`money.finance.sina.com.cn`，不复权，成交量单位为**股**），单位换算在数据层统一归一。
  - **实时快照通道**：采用腾讯财经实时行情接口（`qt.gtimg.cn`），拉取股票与大盘指数当前最新实时成交价、开盘价、最高价、最低价、累计成交量与时间戳。
  - **盘中动态注入机制**：若历史日K列表最新一根日期不等于当天最新交易日（如在交易时段 9:30-15:00 期间），引擎自动将腾讯实时快照构建为当天的最新一根实时 K 线追加合并；若最后一根已是当天，则动态刷新其最新价格、量能与换手率。🆕 **v2.1 零值防御**：未开盘/停牌时接口返回 `"0.00"` 高低价（truthy 字符串），引擎显式校验四价与成交量有效后才会注入，彻底避免 0 值K线污染 ATR/筹码/坐标轴。
  - **权威元数据字典**：内置 `STOCK_META_MAP` 保证核心标的 100% 正确映射中文名称与所属行业，字典外标的自动回退实时列表名称。

### 🆕 v2.0/v2.1 数据层改进

- **真实流通股本换手率计算 (v2.1 单位修正)**：
  - 通过腾讯完整行情接口获取流通市值（`parts[44]` 字段，**单位亿元**——v2.0 误按万元换算导致流通股本偏差一万倍，已修正），除以现价反算流通股本：
    $$FloatShares = \frac{FloatCap_{亿元} \times 10^8}{Price}$$
  - 反算结果额外做合理区间校验（$10^6 \sim 5\times10^{11}$ 股），异常时回退。
  - 🆕 **v2.1**：实时快照优先采用腾讯官方换手率字段（`parts[38]`），缺失时按 `Volume(股) / FloatShares × 100` 反算。**实时快照成交量字段统一按"手×100→股"换算**（v2.0 漏乘 100 导致当日换手率/量比被低估百倍，已修正）。
  - 流通股本数据带缓存机制，避免重复请求。
  - **回退策略**：接口异常时默认 5 亿流通股本（主板合理估计）。

- **涨跌停板检测**：
  - 自动识别主板（10%）与创业板/科创板（20%）涨跌停幅度。
  - 每根K线标记 `is_limit_up` / `is_limit_down` 字段。
  - 判定逻辑：涨跌幅达到限额 ±0.5% 范围内且价格贴近涨跌停价。

- **大盘指数完整OHLC**：
  - IndexEngine 改用腾讯完整行情接口（非简化 `s_` 接口），获取指数的完整开盘/最高/最低/收盘四价，含零值防御。

- 🆕 **v2.1 全A股真实股票列表**：
  - 主通道：新浪 `Market_Center.getHQNodeData`（node=hs_a）按成交额降序分页抓取全A股，取前 240 只活跃标的（过滤 ST/退市/价格<2元/成交额<1000万）。
  - v2.0 及以前仅为 40 只内置字典，"全市场扫描"名不副实，已修正；内置字典保留作为行情源整体失效时的回退。

---

## 3. 多维技术指标与特征提取算法体系

### 3.1 筹码衰减分布模型 (Volume Profile & Chip Distribution)
- **数学原理**：
  将股票历史价格区间 $[P_{\min}, P_{\max}]$ 划分为 $N = 100$ 个等宽价格槽（Price Bins）。设第 $t$ 日换手率为 $Turnover_t \in [0, 1]$，当日价格区间为 $[Low_t, High_t]$，🆕 **v2.1 回看窗口扩至 250 个交易日**（v2.0 仅 120 日，低换手蓝筹的远期筹码被整体丢弃）。
  1. **历史筹码衰减**：
     $$C_t(p) = C_{t-1}(p) \times (1 - Turnover_t)$$
  2. **新增筹码三角分布注入**：
     以当日四价均值 $AvgP_t = \frac{Open_t + Close_t + High_t + Low_t}{4}$ 为顶点，在 $[Low_t, High_t]$ 之间按三角分布赋予权重 $W(p)$，注入新增筹码：
     $$C_t(p) \leftarrow C_t(p) + Turnover_t \times W(p) \times InjectWeight$$
     - 🆕 **v2.0 涨跌停降权**：当 $IsLimitUp_t$ 或 $IsLimitDown_t$ 时，$InjectWeight = 0.3$（涨跌停日筹码分布不可靠，降低注入权重），否则 $InjectWeight = 1.0$。
     - 🆕 **v2.1 指数退化模式**：大盘指数无真实换手率，自动退化为按成交量加权的 Volume Profile（无量衰减），并在支撑带来源中如实标注，不再使用恒定 1.0 的伪造换手率。
  3. **关键特征输出**：
     - **主筹码峰 (POC, Point of Control)**：$POC = \arg\max_p C(p)$（全市场筹码最密集的成本中心）。
     - **当前获利盘比例 (Profit Ratio)**：
       $$ProfitRatio = \frac{\sum_{p \le P_{current}} C(p)}{\sum C(p)} \times 100\%$$
     - 🆕 **v2.0 修正的筹码集中度**：计算包含中间 90%/70% 筹码的价格区间宽度与**现价**比值：
       $$Concentration_{90} = \frac{P_{high,90} - P_{low,90}}{P_{current}} \times 100\%$$
       集中度 $< 10\%$ 为单峰主力高度控盘。

### 3.2 形态几何与空间分割 (Price Structure & Fibonacci)
- **顶底分型与波段极值**：基于滑动窗口（前后各 3 根 K 线）识别局部高点（Swing Highs）与局部低点（Swing Lows）。
- **跳空缺口追踪 (Gap Tracking)**：
  - 上涨未补缺口（$Low_t > High_{t-1}$）：构成下方的极强物理支撑。
  - 下跌未补缺口（$High_t < Low_{t-1}$）：构成上方的极强阻力。
- 🆕 **v2.0 自适应斐波那契黄金分割 (Adaptive Fibonacci Retracement)**：
  - **旧版**：固定取最近 60 根K线的高低点。
  - **改进**：使用简化 ZigZag 算法，在最近 120 根K线中自动识别主波段起止点（全局最高/最低点），并判断主波段方向。
  - 当波段振幅 $< 5\%$ 时自动**扩大搜索窗口至全部历史数据**重新寻找主波段（🆕 v2.1 修复：v2.0 实现误将窗口缩小到 60 根，与文档意图相反）。
  - 黄金分割位计算：
    $$Fib_r = H_{wave} - (H_{wave} - L_{wave}) \times r, \quad r \in \{0.236, 0.382, 0.5, 0.618, 0.786\}$$

### 3.3 动态均线与通道系统 (Dynamic Moving Levels)
- **均线多级别矩阵**：
  - 日线：MA5 (短线爆发)、MA10 (短线持仓)、MA20 (月线生命线)、MA60 (中线生命线)、MA120 (半年线)、MA250 (年线牛熊分界)。
  - 周线：周 MA20 (大级别主升浪生命线)、周 MA60 (大级别强牛熊线)。
- **自适应布林带 (Bollinger Bands)**：
  $$Mid = MA20, \quad Upper = Mid + 2\sigma, \quad Lower = Mid - 2\sigma$$

### 3.4 量价异动与动量背离 (Momentum & Divergence)
- 🆕 **v2.0 MACD DIF极值背离判定**（增强版）：
  - **底背离（看涨反转）**：近 40 天内，同时独立检测：
    1. 价格形成双底且二次探底创新低（$Close_{t2} \le Close_{t1}$）
    2. 在价格极值附近（±3根K线范围）找到 DIF 的局部极小值点
    3. DIF 极值显著抬高：$DIF_{min,t2} > DIF_{min,t1} + \epsilon$，🆕 **v2.1 自适应阈值** $\epsilon = \max(0.03,\ 0.05 \times ATR_{14})$（v2.0 固定 0.03 对高价股是噪声、对低价股永不触发）
    4. 🆕 **v2.1 双底最小间隔**：两个价格极值点至少间隔 5 根K线（$t_2 - t_1 \ge 5$），杜绝相邻K线误判为双底
  - **顶背离（看跌减仓）**：价格冲高破新高，但 DIF 极值点动能衰减降低（阈值同上）。
  - **改进意义**：旧版仅用收盘价极值点的 DIF 值，新版独立检测 DIF 的局部极值，更准确地反映动能变化。
- **KDJ 极限状态识别**（🆕 **趋势过滤增强**）：
  - $J < 20$ 且 **趋势分 ≥ 45**（非空头趋势）：极度超卖酝酿强反弹，+15分。
  - $J < 20$ 但 **趋势分 < 45**（空头趋势下）：超卖效果减弱，仅 +5分。
  - $J > 95$（极度超买面临回调）。
- **成交量倍量与缩量特征**：放量突破（$Vol \ge 1.8 \times VolMA5_{前5日}$）与缩量回踩（$Vol < 0.65 \times VolMA5_{前5日}$）。🆕 **v2.1 量比口径统一**：$VolMA5$ 一律取**前5日均量（不含当日）**，即 $\frac{Vol_{t-1}+\dots+Vol_{t-5}}{5}$；v2.0 分母混入当日量导致 1.8× 阈值实际等效 2.25×，且预测引擎与扫描器两处口径不一致，已全部统一。

### 3.5 🆕 量价特征分析引擎 (Volume-Price Feature Analysis) *v2.0新增*

独立的量价匹配度检测模块，输出以下关键特征：

| 特征标识 | 检测条件 | 信号含义 |
|:---|:---|:---|
| **放量突破** (`is_volume_breakout`) | $VolRatio \ge 1.8$ 且 涨幅 $> 2\%$ | 主力资金强势进攻 |
| **缩量回踩** (`is_shrink_pullback`) | $VolRatio < 0.65$ 且 跌幅 $< 3\%$ | 健康技术调整，卖压耗尽 |
| **量价背离·看跌** (`bearish_vp_divergence`) | 近3日价格上涨但成交量萎缩 $\ge 25\%$ | 放量滞涨，主力出货警惕 |
| **量价背离·看涨** (`bullish_vp_divergence`) | 近3日价格下跌但成交量萎缩 $\ge 25\%$ | 缩量不跌，卖压殆尽 |
| **恐慌抛售** (`panic_selling`) | 近3日价格下跌且成交量放大 $\ge 30\%$ | 恐慌抛售，短线慎入 |
| **连续放量天数** (`consecutive_heavy_vol`) | 连续 N 天 $Vol > 1.5 \times VolMA5$ | 持续资金关注度 |

---

## 4. 多维支撑位/压力位"价格带聚类共振算法"

### 算法核心步骤：
1. **候选池采集**：汇总所有均线、布林轨、筹码主次峰、前高前低、未补缺口、斐波那契点位，按 $< P_{current}$（支撑）与 $> P_{current}$（压力）分类。
   - 🆕 **v2.4 去重规则**：`boll_mid` 恒等于 `ma_20`（日/周均如此），**不得**与 MA20 同时作为独立候选，否则同一价位被双重计分（原日 MA20=20 + 布林中轨=15 → 35 分虚高）。候选池保留 MA20 与布林上/下轨，剔除布林中轨。
2. **邻域密度聚类 (Density Zone Clustering)**：
   设价格容差门限 $\epsilon = 1.5\%$。遍历候选点，当两点相对差距 $\le \epsilon$ 时归入同一价格带：
   $$CenterPrice = \frac{\sum P_i \times W_i}{\sum W_i}, \quad Range = [\min(P_i), \max(P_i)]$$
3. **加权共振评分 (Confluence Scoring)**：
   - 筹码主密集峰 POC：基础分 +35
   - 大周期生命线 (MA60/MA120/周MA20/MA60)：基础分 +28~32
   - 关键结构位/未补缺口：基础分 +24~26
   - 斐波那契黄金分割位/布林轨：基础分 +18~22
   - **多维度共振加成**：包含跨类别特征（如均线+筹码+缺口三维共振）时获得额外权重加成。
4. **输出分级**：
   - 得分 $\ge 75$：⭐⭐⭐⭐⭐ 极强共振带
   - 得分 $55 \sim 74$：⭐⭐⭐⭐ 强共振带
   - 得分 $38 \sim 54$：⭐⭐⭐ 中度有效带
   - 自动按离现价距离输出 $S_1, S_2, S_3$（核心支撑）与 $R_1, R_2, R_3$（核心压力）。

---

## 5. 大概率走势预测与量化交易计划生成引擎

### 5.1 周K+日K双周期共振模型
遵循专业交易铁律：**"周K定大趋势方向，日K定波段买卖点"**。
- **周K主升多头**：周 $Close > MA20 > MA60$ 且周 MACD 红柱，赋予多头大级别趋势加权。
- **日K回踩/突破**：在周线向上的背景下，日K缩量回踩 $S_1$ 强支撑带或放量突破 $R_1$ 压力带时触发高胜率买入。

### 5.2 🆕 自适应四维特征量化评分机制 *(v2.0改进)*
系统从四个独立维度进行**自适应加权**评分（0~100 分）：

1. **趋势大势分 (Trend Score)**：日周均线排列与多头均线发散程度。
   - 完整多头排列（$Close > MA20 > MA60 > MA120$）额外加分。
2. **筹码沉淀分 (Chips Score)**：获利盘比例、单峰集中度、现价与主力成本线贴合度。
3. **动量背离分 (Momentum Score)**：MACD底背离、KDJ金叉/超卖、🆕量价匹配度（放量突破/缩量回踩/量价背离子项）。
4. **空间位置分 (Position Score)**：现价距离强支撑带 $S_1$ 的贴近程度（最佳区间：$0\% \sim 2.5\%$）与上方离压力带 $R_1$ 的利润空间。

🆕 **v2.0 自适应权重调整**（替代固定等权25%）：

| 市场体制 | 判定条件 | 趋势权重 | 筹码权重 | 动量权重 | 位置权重 |
|:---|:---|:---|:---|:---|:---|
| **多头趋势市** | 周线+日线均线多头排列 | **35%** | 20% | 20% | 25% |
| **空头趋势市** | 周线+日线均线空头排列 | **35%** | 20% | 25% | 20% |
| **反转信号市** | KDJ极端超卖(<20)或超买(>90) | 20% | 25% | **35%** | 20% |
| **低波动震荡市** | ATR/Close < 1.5% | 20% | **30%** | 20% | **30%** |
| **默认** | 以上均不满足 | 25% | 25% | 25% | 25% |

### 5.3 🆕 严格历史相似形态胜率回溯统计 (Historical Backtest) *(v2.0改进)*

- 遍历当前股票历史 250~500 个交易日样本，提取历史中出现**"回踩强支撑 + KDJ超卖/底背离 + 多头趋势"**的相似触发点。
- 🆕 **v2.0 严格统计规则**（🆕 **v2.1 补强**）：
  1. **纯收盘价判断**：胜率仅以 5/10/20 日收盘价判定（不再混入期间最高价）。
  2. **扣除交易成本**：双向交易成本约 1.0%（佣金 + 印花税 + 冲击成本），收益必须覆盖成本后为正才算"胜"。
  3. **无硬编码默认值**：数据不足 60 根 K 线或相似样本 < 5 个时，返回 `insufficient_data` 状态，前端显示"样本不足，无法计算有效胜率"，**绝不虚构数据**。🆕 **v2.1**：v2.0 前端存在 `|| 70` 兜底导致样本不足时仍显示 70%/75%/78% 假胜率，已彻底移除，改为显式"样本不足"提示。
  4. 🆕 **v2.1 退化防护**：三个相似条件（回踩支撑/超卖/多头趋势）全部未激活时，"相似形态"将退化为全样本基础概率统计，此时直接返回 `insufficient_data` 而非输出无意义胜率。
  5. 🆕 **v2.1 样本去重叠**：相邻历史触发点至少间隔 5 根K线，降低窗口重叠带来的样本自相关与置信度虚高。
  6. 🆕 **v2.1 置信度加权融合**：回测胜率与综合评分的融合权重按样本量缩放 $w = 0.5 \times \min(1,\ n/30)$——样本不足 30 个时按比例降低胜率话语权，杜绝小样本噪声（如 5 个样本的 100% 胜率）以 50% 权重扭曲最终预测；$n \ge 30$ 时达到 0.5 的满额权重。
- 统计输出：
  - **5日净收益胜率（%）** *(扣费后)*
  - **10日净收益胜率（%）** *(扣费后)*
  - **20日净收益胜率（%）** *(扣费后)*
  - **平均10日净收益幅度（%）** *(扣费后)*
  - **有效样本数量**

### 5.4 🆕 结构化量化交易计划卡 (Trade Plan) 生成 *(v2.0改进)*
自动生成严格符合专业风控标准的交易计划：
- **建议建仓区间**：$[S_{1,\text{center}} \times 0.992, \max(P_{\text{current}} \times 1.003, S_{1,\text{center}} \times 1.015)]$
- **第一目标止盈位 (TP1)**：取第一**强**压力带（星级 $\ge 3$）中心价 $R_{strong}$，计算预期盈利空间（🆕 扣除双向交易成本）：
  $$Gain\% = \frac{R_{strong} - P}{P} \times 100\% - CostRate_{双向}$$
  🆕 **v2.1 修正**：v2.0 取"最近压力带"做 TP1——最近聚类常为现价上方 <1.5% 的 MA5/布林上轨微阻力簇（纯权重堆叠即可达3星），导致盈亏比恒趋近 0、评级体系失效。现按文档本意取第一**强**压力带，并施加**成本经济学下限**：目标距现价须 $\ge 3\%$（双向成本 1% 下，<3% 的目标净收益 <2%，结构上不可能达到有效盈亏比）；无符合条件者依次回退：最近强压力带 → 现价+8%。TP2 取 TP1 之上次一有效目标。
- 🆕 **ATR 动态止损位 (SL)**（v2.0改进，🆕 **v2.1 语义修正**）：取以下两种止损中**价格更高者**（= 风险暴露更小的真正"保守"值；v2.0 误用 min 取了更低价格即更宽止损，与"保守"意图相反）：
  1. 固定比例止损：$SL_{fixed} = S_1 \times 0.975$（跌破支撑下沿2.5%）
  2. ATR 动态止损：$SL_{ATR} = Entry_{low} - 2 \times ATR_{14}$
  $$SL = \max(SL_{fixed}, SL_{ATR})$$
  风险暴露（🆕 含交易成本）：$Risk\% = \frac{P - SL}{P} \times 100\% + CostRate_{双向}$
- **期望盈亏比 (R:R Ratio)**：
  $$R:R = \frac{Gain\%}{Risk\%}$$
  - $R:R \ge 3.0 : 1$：评定为【极佳】
  - $R:R \ge 2.0 : 1$：评定为【良好】

---

## 6. 全A股盘后批量扫描与选股雷达

通过多线程并发执行引擎，实现盘后**全市场活跃池**（🆕 v2.1：新浪全A股列表按成交额降序前 240 只，v2.0 仅为 40 只内置字典）高速扫描，支持四大高胜率量化策略池。🆕 **v2.1**：策略匹配统一收敛为 `ScannerEngine.match_strategies`，盘后扫描与演示数据共用同一套严格条件（v2.0 的演示回退将概率≥60 的标的全部打上"回踩强支撑"标签，已移除），演示数据带 `is_demo` 标记并在前端显示"演示"角标。

| 策略标识 | 策略名称 | 核心量化筛选规则 *(v2.0 完善)* | 适用周期与偏好 |
| :--- | :--- | :--- | :--- |
| **`SUPPORT_PULLBACK`** | **短线·回踩强支撑** | 现价处于 $S_1$ 上方 $\le 2.5\%$，支撑星级 $\ge 3$ 星，KDJ $J < 30$ 或信号为 `BUY_SUPPORT_PULLBACK`，10日胜率 $\ge 70\%$（有充足样本时），盈亏比 $\ge 2.2:1$ | 短线 3~10 个交易日 |
| **`BREAKOUT_PRESSURE`** | **短线·放量突破** | 今日放量 $\ge 1.6 \times VolMA5$，逼近 $R_1$（距离 $\le 2\%$），筹码获利盘 $\ge 70\%$ | 短线爆发力追强 |
| **`MAIN_WAVE_TREND`** | **中线·主升浪起爆** | 周K主升多头排列 + 90%筹码集中度 $\le 10\%$ (单峰控盘) + 现价高于POC $\ge 2\%$（突破中枢）+ 胜率 $\ge 70\%$ | 中线 1~3 个月主升浪 |
| **`OVERSOLD_DIVERGENCE`** | **超跌·多重底背离** | 触及布林下轨（$Price \le BollLower \times 1.01$）或日线 MACD 底背离，KDJ $J < 15$（极度超卖企稳） | 左侧/波段反弹抄底 |

---

## 7. 大盘核心指数多周期走势研判与操作许可量化系统

针对 **上证指数 (000001)**、**深证成指 (399001)**、**创业板指 (399006)** 与 **科创50 (000688)**，系统建立了专门的大盘多周期研判引擎（`IndexEngine`）。

### 7.1 四大周期 (30分/60分/日线/周线) 结构剖析
- **30分钟**：捕捉日内与超短线的最快入场节拍，判断日内脉冲动量；
- **60分钟**：确认盘中反弹/调整结构能否延续至收盘，以及与日线的共振关系；
- **日线**：短线与波段交易核心基准，决定是否触发个股交易动作；
- **周线**：中长期战略方向基准，决定账户总仓位上限与牛熊底色。

### 7.2 周期价格斜率 (ATR/根) 与方向分 G 评分模型
1. **价格斜率 (Slope, ATR/根)**：
   衡量近 5 根 K 线的平均价格推进速度与波动率 ATR 的比值：
   $$Slope = \frac{(Close_t - Close_{t-4}) / 4}{ATR_t}$$
2. **周期多空方向分 $G$ (Direction Score, $-1.0 \sim +1.0$)**：
   - 🆕 **v2.2 MA20/MA60 连续化**：$G_{MA20} = 0.30 \times \tanh\left(\frac{Close - MA20}{0.5 \times ATR}\right)$，$G_{MA60} = 0.20 \times \tanh\left(\frac{MA20 - MA60}{0.5 \times ATR}\right)$，消除均线附近阶跃抖动。
   - 🆕 **v2.4 MACD 单项计分（修复重复计分）**：因 $Hist = 2 \times (DIF - DEA)$，`Hist > 0` 与 `DIF > DEA` 完全等价，**只能计一次**。规则：
     - 红柱且柱体抬升（$Hist > 0$ 且 $Hist \ge Hist_{prev}$）：$+0.35$
     - 红柱但收敛：$+0.15$
     - 绿柱且柱体加深（$Hist < 0$ 且 $Hist \le Hist_{prev}$）：$-0.35$
     - 绿柱但收敛：$-0.15$
   - KDJ $J < 30$ 超卖反弹潜能：$+0.10$；$J > 90$ 超买：$-0.15$
   - 输出标准化方向分 $G \in [-0.95, +0.95]$

### 7.3 反弹条件与回调风险量化评分
- **反弹条件评分 (Rebound Condition, 0~30)**：基于超卖程度 $Rebound = \max(0, (40 - J) \times 0.4)$，数值越高代表短线超跌反弹动能越强；
- **回调风险评分 (Pullback Risk, 0~30)**：基于超买程度与正向斜率 $Risk = \min(28, \max(5, (J - 40) \times 0.35 + 5 \times Slope))$，数值越高代表冲高遇阻回调风险越大。

### 7.4 正式操作许可与量化建议仓位决策矩阵

> 🆕 **v2.4 分支顺序修正**：判定按以下**严格优先级**执行（`if/elif` 链），避免"周线弱多 + 日线走空 + 60分微正"被误判为震荡蓄势可加仓。

| 优先级 | 多周期共振条件 | 正式操作许可 | 建议总体仓位 |
| :---: | :--- | :--- | :--- |
| 1 | **周线 $G \ge 0.2$ 且 日线 $G \ge 0.2$** | **【多头顺势，逢低做多】** | **70% ~ 85%** |
| 2 | **周线 $G < 0$ 且 日线 $G < 0$** | **【空头承压，防守观望】** | **10% ~ 30%** |
| 3 | **周日线方向冲突**：$G_{weekly} \times G_{daily} < 0$ 且 $\min(\|G_w\|, \|G_d\|) \ge 0.1$ | **【大方向不明，先观望】** | **30% ~ 50%** |
| 4 | **震荡蓄势**：周线 $G \ge 0.1$ **且 日线 $G \ge 0$** 且 60分 $G > 0$ | **【震荡蓄势，区间波段】** | **50% ~ 65%** |
| 5 | 以上均不满足 | **【大方向不明，先观望】** | **30% ~ 50%** |

v2.4 相对 v2.3 的关键变化：
- **"震荡蓄势"新增日线非负约束**（$G_{daily} \ge 0$），60 分单独为正不再足够；
- **"周日线冲突"判定提前**，排在震荡蓄势之前，与表格第 3 行文档意图一致。

### 7.5 大盘专属支撑/压力位与防守底线
- 自动提取大盘在日K与周K上的关键密集价格带（🆕 **v2.1 说明**：大盘聚类容差用 1.2%，较个股的 1.5% 更紧，因指数点位波动率低于个股；指数筹码候选来自成交量分布模型，非换手率筹码），计算输出：
  - **核心支撑带 $S_1, S_2, S_3$**（如 3940 点、3910 点，附带均线/缺口来源与星级）；
  - **核心压力带 $R_1, R_2, R_3$**（如 3985 点、4020 点）；
- 在结论中明确给出**"向上突破观察点"**与**"向下极限防守线"**。

### 7.6 🆕 多周期方向摘要 API (`timeframes`) 与 MRDI 数据契约 *(v2.4)*

`/api/index/analysis` 在 v2.4 起额外返回两个字段，供 **MRDI 面板**与前端多周期决策矩阵直接使用，避免前端误解析 `periods` 数组或截断 K 线：

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `daily_kline_full` | `Kline[]` | 完整日 K（约 **250 根**），供 MRDI 的 RD/CD/G 与"近 250 日分位"计算；`kline_data` 仍为图表截断的 **90 根** |
| `timeframes` | `object` | 五周期方向摘要，键名：`monthly` / `weekly` / `daily` / `60m` / `30m` |

每个 `timeframes.*` 条目结构：

```json
{
  "label": "偏多 | 弱偏多 | 中性 | 弱偏空 | 偏空 | 数据不足",
  "score": 0.35,
  "status_tag": "上行延续",
  "detail": "结构偏多 · MACD零轴上 · 红柱放大 · 动能分 +0.35"
}
```

**月线 (`monthly`)** 特殊说明：新浪 K 线接口无月线周期，由 `IndexEngine._monthly_trend()` 对周 K **重采样为月 K**（`MS` 频率），用 收盘 vs 月MA6 / 月MA6 vs 月MA12 / 近3月涨跌幅 合成方向分。

**前端 MRDI 消费约定**（详见 [MRDI 实施总结](./MRDI%20动量共振与多周期决策模型%20—%20实施总结.md)）：
- RD/CD/G 在 `daily_kline_full` 上计算，**禁止**使用 `kline_data`（90 根会导致 MACD/KDJ 预热不足、分位数名不副实）；
- 多周期矩阵从 `timeframes` 读取，**禁止**用 `periods["1200"]`（`periods` 是数组而非对象）。

---

## 8. 系统工程实现与可视化看板交付

### 8.1 技术栈总览
- **后端**：Python 3.12 + FastAPI + Pandas + NumPy + SciPy + Requests
- **前端**：Vanilla HTML5 + Modern CSS3 (深色玻璃拟态) + JavaScript + ECharts 5.5
- **数据通道**：免Token聚合引擎（新浪财经K线 + 腾讯财经实时快照）

### 8.2 核心代码文件索引
1. **数据引擎**：[`backend/data_fetcher.py`](file:///e:/code/A/backend/data_fetcher.py)
2. **大盘指数研判引擎**：[`backend/index_engine.py`](file:///e:/code/A/backend/index_engine.py)
3. **指标特征引擎**：[`backend/indicator_engine.py`](file:///e:/code/A/backend/indicator_engine.py)
4. **价格带聚类引擎**：[`backend/cluster_engine.py`](file:///e:/code/A/backend/cluster_engine.py)
5. **预测与交易计划引擎**：[`backend/prediction_engine.py`](file:///e:/code/A/backend/prediction_engine.py)
6. **全市场批量扫描器**：[`backend/scanner_engine.py`](file:///e:/code/A/backend/scanner_engine.py)
7. **API 服务入口**：[`backend/app.py`](file:///e:/code/A/backend/app.py)
8. **前端交互看板**：[`frontend/index.html`](file:///e:/code/A/frontend/index.html)、[`frontend/style.css`](file:///e:/code/A/frontend/style.css)、[`frontend/app.js`](file:///e:/code/A/frontend/app.js)

### 8.3 本地运行指令
```bash
# 进入后端目录
cd e:\code\A\backend

# 启动服务
python app.py
```
浏览器访问 **`http://127.0.0.1:8000`** 即可在 **个股量化看板**、**大盘多周期研判** 与 **MRDI 动量共振决策** 三个工作区之间切换。

### 8.4 🆕 MRDI 动量共振与多周期决策面板 *(v2.4)*

第三个工作区 **MRDI（Momentum Resonance & Decision Intelligence）**，参考 [shorkamin.com/financial-analysis](https://www.shorkamin.com/financial-analysis/) 设计理念，与大盘研判共用 `/api/index/analysis`，在前端完成 RD/CD/G 计算与决策驾驶舱渲染。

| 模块 | 职责 |
|:---|:---|
| **RD 反弹需求** | 7 因子加权（RSI/KDJ/乖离/MACD柱收窄/连跌/缩量超跌/5日跌幅），满分约 114 |
| **CD 回调压力** | 对称 7 因子（超买/正乖离/红柱收窄/连涨/放量冲高等） |
| **G 拐点触发** | MACD 金叉/死叉 ± KDJ 金叉/死叉 ± 2 日价格动量；MACD 连续项按 **ATR 归一化**并夹逼 **[-1, 1]** |
| **多周期矩阵** | 月线定级别上限 → 周线定操作许可 → 日线定阶段 → 60 分确认延续 → 30 分捕捉时点 |

v2.4 算法闭环要点：
- `scoreMrdiRdCd()` 为 RD/CD **唯一打分函数**，当前值与"近 250 日分位"必须共用，禁止简化公式算分位；
- 确认阶梯的 30/60 分钟状态取自后端 `timeframes["30m"]` / `timeframes["60m"]`，不再用占位文案或日线 G 近似。

完整 UI 结构、验证用例与 v1.1 修复清单见独立文档：[MRDI 动量共振与多周期决策模型 — 实施总结.md](./MRDI%20动量共振与多周期决策模型%20—%20实施总结.md)

---

## 9. 🆕 v2.0 改进日志

### 致命级修复 (P0)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P0-1 | 换手率使用 `vol/1000000` 伪造 | 改为通过腾讯接口获取流通市值反算真实流通股本 | `data_fetcher.py` |
| P0-2 | 回测数据不足时返回硬编码72-79%胜率 | 不足时返回 `insufficient_data`，前端显示"样本不足" | `prediction_engine.py` |
| P0-3 | 5日胜率混入10日最高价条件 | 严格用收盘价判断，扣除1%双向交易成本 | `prediction_engine.py` |

### 重要修复 (P1)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P1-1 | IndexEngine实时K线open/high/low全设为close | 使用完整行情接口获取四价 | `index_engine.py` |
| P1-2 | 选股策略条件与文档严重不一致 | 四大策略全部按文档补全筛选条件 | `scanner_engine.py` |
| P1-3 | 筹码集中度公式偏差 `/(p_h+p_l)` | 修正为 `/current_price` | `indicator_engine.py` |
| P1-4 | Demo数据无标记，用户无法区分 | 添加 `is_demo: true` 标记 + 恢复严格匹配 | `app.py` |

### 功能增强 (P2)

| 编号 | 改进 | 说明 | 影响文件 |
|:---|:---|:---|:---|
| P2-1 | 涨跌停检测与K线标记 | 每根K线标记涨跌停状态，筹码模型降权处理 | `data_fetcher.py`, `indicator_engine.py` |
| P2-2 | 量价特征分析引擎 | 新增放量突破/缩量回踩/量价背离/连续放量检测 | `indicator_engine.py` |
| P2-3 | 量价确认纳入动量评分 | 动量分加入量价匹配度子项 | `prediction_engine.py` |
| P2-4 | ATR动态止损 | 止损取固定比例与ATR止损中更保守者 | `prediction_engine.py` |
| P2-5 | 自适应斐波那契 | 简化ZigZag替代固定60根窗口 | `indicator_engine.py` |

### 算法优化 (P3)

| 编号 | 改进 | 说明 | 影响文件 |
|:---|:---|:---|:---|
| P3-1 | 自适应评分权重 | 根据市场体制动态调整四维评分权重 | `prediction_engine.py` |
| P3-2 | MACD背离DIF极值检测 | 独立检测DIF局部极值而非仅用收盘价极值 | `indicator_engine.py` |
| P3-3 | KDJ趋势过滤 | 空头趋势下KDJ超卖信号降低加分效果 | `prediction_engine.py` |
| P3-4 | 盈亏比含交易成本 | 收益和风险均扣除双向0.5%×2=1%交易成本 | `prediction_engine.py` |

---

## 10. 🆕 v2.1 修正日志 (2026-08-30)

### 致命级修复 (P0)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P0-6 | 实时快照成交量单位错误（漏乘100，手当股用）| 统一"手×100→股"换算常量；当日换手率/量比/筹码衰减不再被污染 | `data_fetcher.py` |
| P0-7 | 流通市值 parts[44] 单位误判（亿元当万元）| 修正为 ×1e8 换算并加合理区间校验（v2.0 流通股本偏差一万倍）| `data_fetcher.py` |
| P0-8 | 盘前/停牌时 "0.00" 价格被解析为 0 值K线注入 | 快照四价与成交量显式校验，零值不注入、不刷新 | `data_fetcher.py`, `index_engine.py` |
| P0-9 | K线为不复权数据，除权缺口污染缺口/筹码/MA | K线主通道切换为腾讯前复权(qfq)接口，新浪降为回退源 | `data_fetcher.py` |
| P0-10 | 前端 `\|\| 70/75/78` 兜底虚构回测胜率（P0-2 修复不彻底）| 移除全部默认值兜底，样本不足时显示"样本不足" | `frontend/app.js` |
| P0-11 | 回测三条件全未激活时退化为全样本统计冒充"相似形态胜率" | 全未激活直接返回 `insufficient_data`；样本去重叠(≥5根间隔) | `prediction_engine.py` |

### 重要修复 (P1)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P1-5 | 量比分母混入当日量，且预测/扫描两处口径不一致 | 统一为"前5日均量(不含当日)"，扫描器直接复用 volume_features | `indicator_engine.py`, `scanner_engine.py` |
| P1-6 | ATR止损 `min()` 取了更宽止损却注释"更保守" | 改为 `max()`（价格更高=风险更小=真正保守）| `prediction_engine.py` |
| P1-7 | MACD背离固定0.03阈值不随价格尺度伸缩 | 自适应阈值 max(0.03, 0.05×ATR) + 双底最小间隔5根 | `indicator_engine.py` |
| P1-8 | 筹码模型仅回看120日、指数用伪造换手率1.0 | 回看扩至250日；指数退化为成交量分布模式 | `indicator_engine.py` |
| P1-9 | "全A股扫描"实为40只内置字典 | 新浪全A列表按成交额降序前240只，字典降为回退 | `data_fetcher.py` |
| P1-10 | 演示数据放宽匹配(概率≥60全打回踩标签)、前端吞掉is_demo | demo走统一严格匹配函数 `match_strategies`，前端显示"演示"角标 | `app.py`, `frontend/app.js` |
| P1-11 | 扫描无finally保护，异常后 is_scanning 永久锁死、前端无限轮询 | try/finally 复位状态；前端轮询失败自动退出 | `scanner_engine.py`, `frontend/app.js` |
| P1-12 | 大盘K线tooltip蜡烛图索引错位，hover即TypeError崩溃 | 按 ECharts 蜡烛图原始数据序 [open,close,low,high] 取值 | `frontend/app.js` |
| P1-13 | 周期卡片状态色用CSS变量拼接透明度后缀导致背景失效 | 后端改返回hex色值 | `index_engine.py` |

### 算法对齐与增强 (P2)

| 编号 | 改进 | 说明 | 影响文件 |
|:---|:---|:---|:---|
| P2-6 | 自适应斐波那契"扩窗"实现与文档相反 | 振幅<5%时扩大到全部历史重新搜索 | `indicator_engine.py` |
| P2-7 | 单峰集中度阈值不一致(is_single_peak用12%) | 统一为10%，与评分引擎/文档一致 | `indicator_engine.py` |
| P2-8 | 位置分最佳区间2.8%与文档2.5%不符 | 统一为2.5% | `prediction_engine.py` |
| P2-9 | 量价背离"近3日"实为隔2日首尾比较 | 明确为"当前 vs 3个交易日前"口径并修文档 | `indicator_engine.py` |
| P2-10 | G评分"红柱发散/金叉"仅判状态未判方向变化 | 红柱需柱体抬升(≥前一根)才给满0.25，收敛0.10 | `index_engine.py` |
| P2-11 | MAIN_WAVE"胜率≥70%"误用综合分 | 改用回测胜率门槛(有样本时) | `scanner_engine.py` |
| P2-12 | 超跌背离策略无前端入口 | 补充 OVERSOLD_DIVERGENCE 策略tab | `frontend/index.html` |
| P2-13 | CORS `*` + credentials 不合规、uvicorn reload=True 交付默认 | allow_credentials=False；reload 关闭 | `app.py` |
| P2-14 | POC高亮固定0.2元容差对高/低价股失效 | 前端改为筹码占比最大值(argmax)判定 | `frontend/app.js` |
| P2-15 | 代码徽章恒显 .SH/SZ、日线当日量不刷新等小项 | 按代码前缀推导 .SH/.SZ/.BJ；实时合并时刷新量能 | `frontend/app.js`, `data_fetcher.py` |
| P2-16 | 次新股(<60根K线)输出半残预测(MA60为NaN仍给高分概率) | 预测与个股分析门槛统一为60根，不足时404"数据不足"，与回测门槛对齐 | `prediction_engine.py`, `scanner_engine.py` |
| P2-17 | 小样本回测胜率以50%权重扭曲最终概率 | 融合权重按样本量置信度缩放 0.5×min(1, n/30) | `prediction_engine.py` |

> **遗留说明**：历史相似形态以"MA20±2.5%"代理"回踩强支撑S1"（S1为动态聚类结果无法逐日历史化），两者定义存在近似偏差，属模型局限而非缺陷；筹码模型为250日截断估计，非全历史精确分布。

---

## 11. 🆕 v2.2 修正日志 (2026-08-31)

### 致命级修复 (P0)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P0-12 | 回测仅比较终点收盘价，忽略持仓期间盘中是否触碰止损（路径无关伪胜率）| 引入路径依赖止损模拟：持仓期间逐日检查`low`是否触碰 $SL = P - 2 \times ATR$ 动态止损线，触碰则以止损价结算为亏损 | `prediction_engine.py` |
| P0-13 | 盈亏比（R:R）在 `expected_loss_pct ≤ 0` 时硬编码为 3.0（极佳），破位跳空场景给出错误风控信号 | `expected_loss_pct ≤ 0` 或 `expected_gain_pct ≤ 0` 时 R:R 置零，标记为"异常（禁止入场）" | `prediction_engine.py` |

### 重要修复 (P1)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P1-14 | 回测样本去重叠间隔仅5根K线，10日/20日持仓窗口重叠度达50%/75%，导致等效独立样本量虚高 | 最小样本间隔从5提升至10根K线，将10日窗口重叠度降至0% | `prediction_engine.py` |
| P1-15 | MACD背离极值检测使用`i-3:i+4`前视窗口，最后2根K线永远无法被标记为极值，信号延迟2-3天 | 对末端2根K线采用左侧单边确认（仅需左侧3根），未确认极值使用 $1.5\times$ 更严格阈值降低假阳性 | `indicator_engine.py` |
| P1-16 | 价格带聚类的链式滚雪球效应：连续分布的候选价格（如每两个相距1.3%）可被增量均值合并为跨度远超1.5%容差的巨型Cluster | 新增绝对最大跨度约束：单Cluster跨度不得超过 $2 \times tolerance\_pct \times P_{anchor}$，超出强制切分 | `cluster_engine.py` |

### 算法优化 (P2)

| 编号 | 改进 | 说明 | 影响文件 |
|:---|:---|:---|:---|
| P2-18 | 大盘方向分G的MA20/MA60位置判定为离散阶跃（±0.30跳变），价格在均线附近窄幅波动时操作许可级别频繁闪烁 | 改为ATR标准化的连续平滑函数 $G_{MA20} = 0.30 \times \tanh\left(\frac{Close - MA20}{0.5 \times ATR}\right)$，消除临界点抖动 | `index_engine.py` |

---

## 12. 🆕 v2.3 大盘核心指数支撑/压力精确计算与模型升级 (2026-08-31)

### 致命级与关键模型修复

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P0-14 | 聚类打标仅给前4级打标（`supports[:4]`），第5级支撑/压力带丢失 label 变为 `[None]` / `undefined` | 修复为 `supports[:5]` 和 `resistances[:5]` 完整打标，彻底消除第5级缺失 | `cluster_engine.py` |
| P0-15 | 支撑/压力区分设置了 0.4% 死区（`< 0.998` / `> 1.002`），导致正处在关键均线上的候选点被双向丢弃 | 将死区缩减为 0.1%（`<= 0.9995` / `>= 1.0005`），确保临界均线不丢失 | `cluster_engine.py` |
| P1-17 | 跳空缺口阈值固定0.5%过大，大盘指数中极具战略意义的未补缺口（8-15点）全部被过滤 | 优化为0.2%自适应阈值，精准捕获大盘向上/向下突破未补缺口 | `indicator_engine.py` |
| P1-18 | 周K线仅包含4条均线（MA20/60/布林轨），缺少周MA5/10/30/120/250等中长线牛熊生命线 | 扩充周线全级别均线池（周MA5/10/20/30/60/120/250/布林中轨）| `cluster_engine.py` |
| P1-19 | 大盘分析未吸收 60分钟分时波段共振，短线支撑压力精度不足 | `ClusterEngine` 新增 `indicators_60m` 支持，吸收60分MA20/MA60/MA120/布林轨分时共振 | `cluster_engine.py`, `index_engine.py` |
| P2-19 | 大盘日K仅拉取180根，无法覆盖完整1年宏观周期（MA250与Volume Profile不准）| 日K拉取提升至250根（完整1年交易日），周K提升至120根；结论段落联动S1/S2与R1/R2 | `index_engine.py` |

> **v2.4 补充说明**：P1-18 新增的「周线布林中轨」候选在 v2.4 因与周 MA20 等价被剔除（见 §13 P0-20），候选池保留周 MA20 与布林上/下轨。

---

## 13. 🆕 v2.4 MRDI 多周期闭环与算法一致性修正 (2026-09-02)

> **背景**：代码审查发现 MRDI 面板 UI 冒烟通过但多周期决策链路存在 P0 级逻辑缺陷；大盘 G 分与聚类引擎存在重复计分；操作许可分支顺序与文档表格不一致。本节与 [MRDI 实施总结 v1.1](./MRDI%20动量共振与多周期决策模型%20—%20实施总结.md#v11-算法与链路修正-2026-09-02) 形成闭环知识库。

### 致命级修复 (P0)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P0-16 | MRDI 多周期矩阵：`periods` 为数组，前端误用 `periods["1200"]` 导致周线/月线**永远为"中性"** | 后端新增 `timeframes` 五周期方向摘要；前端 `calculateMultiTimeframeDirection()` 改从该字段读取 | `index_engine.py`, `app.js` |
| P0-17 | MRDI 在 **90 根** `kline_data` 上计算 RD/CD/G，"近250日分位"名不副实，MACD/KDJ 预热不足 | 后端新增 `daily_kline_full`（约 250 根）；MRDI 强制使用该字段 | `index_engine.py`, `app.js` |
| P0-18 | RD/CD **分位数用简化 2 因子公式**，与当前 7 因子满分公式不一致，百分位无统计意义 | 抽取 `scoreMrdiRdCd()` 公共函数，历史分位与当前值**共用同一打分函数** | `app.js` |
| P0-19 | 大盘 G 分：`Hist>0` 与 `DIF>DEA` 等价却叠加 +0.25 与 +0.15，MACD 单项可达 +0.40 | 合并为单项 MACD 柱体发散/收敛计分，量程 ±0.35 / ±0.15（见 §7.2） | `index_engine.py` |
| P0-20 | 聚类：`boll_mid` 与 `ma_20` 同一价位双重计分（日/周均存在） | 候选池剔除 `boll_mid`，保留 MA20 与布林上/下轨（见 §4） | `cluster_engine.py` |

### 重要修复 (P1)

| 编号 | 问题 | 修复方案 | 影响文件 |
|:---|:---|:---|:---|
| P1-20 | 操作许可："震荡蓄势"排在"周日线冲突"之前，且不要求日线非负，易误判可加仓 | 重排分支优先级；震荡蓄势新增 $G_{daily} \ge 0$ 约束（见 §7.4） | `index_engine.py` |
| P1-21 | MRDI 的 G 值：MACD 连续项 `macdMomentum * 2` 无价格量纲归一化，指数与低价股不可比 | 改为 `4 × macdMomentum / ATR` 并夹逼 [-1, 1] | `app.js` |
| P1-22 | MRDI 确认阶梯 30/60 分钟用占位文案或日线 G 近似，非真实分时周期 | 改读 `timeframes["30m"]` / `timeframes["60m"]` 的 score 与 label | `app.js` |
| P1-23 | 驾驶舱"大级别方向"仅显示日线，未体现月线定级别 / 周线定许可设计 | 驾驶舱 major 改为 `周线 X / 月线 Y`；操作许可卡片列出五级矩阵理由 | `app.js` |

### 后端 API 新增字段 (P2)

| 编号 | 改进 | 说明 | 影响文件 |
|:---|:---|:---|:---|
| P2-20 | `timeframes` 多周期方向摘要 | 月(周K重采样)/周/日/60m/30m 的 label、score、status_tag、detail | `index_engine.py` |
| P2-21 | `daily_kline_full` 完整日K | 约 250 根，与图表用 90 根 `kline_data` 分离 | `index_engine.py` |
| P2-22 | `_monthly_trend()` 月线合成 | 周K → 月K resample，月MA6/MA12 + 近3月涨跌幅方向分 | `index_engine.py` |

### 验证记录 (2026-09-02)

| 用例 | 预期 | 实测 |
|:---|:---|:---|
| 上证 `sh000001` | `daily_kline_full` ≈ 251 根；聚类来源无"布林中轨" | ✅ 251 根；S1/R1 正常；无中轨来源 |
| 上证周+0.15 / 日-0.5 / 60分+0.01 | 应判"大方向不明"而非震荡蓄势 | ✅ `op_license` = 大方向不明 |
| 创业板 `sz399006` MRDI | 周线偏空 → permission 暂停观望；RD 分位与 G 在 [-1,1] | ✅ weekly 偏空 -0.35；G=-0.15；82.8% RD 分位 |
| 合成单边上涨序列 G | MACD 单项 ≤ 0.35，总 G ≤ 0.95 | ✅ G = +0.85 |

> **遗留说明（未在本版修复）**：回测胜率仍为单股小样本统计、无 out-of-sample 验证；`bullish_probability` 命名仍易误解为校准概率。参见审查报告中的"下一阶段建议：点时间评估框架 + 横截面 pooled 统计"。

---
